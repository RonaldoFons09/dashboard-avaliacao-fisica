# 🧠 Como Escrever Boas Funções em Python

Este guia prático mostra **quando criar uma função** e **como escrever funções limpas, legíveis e profissionais em Python**.

---

## 🎯 O que é uma função?

Uma função é um **bloco de código reutilizável** que executa **uma tarefa específica**, podendo receber dados de entrada (parâmetros) e devolver um resultado (retorno).

---

## ❓ Quando escrever uma função?

Use funções sempre que um bloco de código:

- Representar **uma ideia clara**
- Puder ser **reutilizado**
- Tiver um **nome descritivo**
- For maior que **5–10 linhas**
- Contiver **regra de negócio**

### Regra mental
> *"Esse código representa uma ideia que pode ter um nome?"*

Se a resposta for sim → **crie uma função**.

---

## 🚨 Sinais claros de que você DEVE criar uma função

### 1️⃣ Código repetido

Se você copiou e colou código, transforme em função.

```python
def calcular_area(base, altura):
    return base * altura
```

---

### 2️⃣ Bloco que faz "uma coisa só"

Se você consegue explicar o bloco em uma frase, ele merece uma função.

```python
def validar_usuario(usuario):
    return usuario.is_active and usuario.idade >= 18
```

---

### 3️⃣ Código difícil de entender sem comentários

Se precisa explicar *o que* o código faz, use uma função com bom nome.

```python
def calcular_total_com_taxa(valor):
    return valor * 1.1
```

---

## ✅ Como escrever uma BOA função

### ✔️ 1. Faça apenas UMA coisa

❌ Ruim:
```python
def processar_pedido():
    validar()
    salvar()
    enviar_email()
```

✅ Bom:
```python
def validar_pedido():
    ...

def salvar_pedido():
    ...

def enviar_email_confirmacao():
    ...
```

---

### ✔️ 2. Use nomes claros e descritivos

❌ Ruim:
```python
def f(x):
    return x * 0.9
```

✅ Bom:
```python
def aplicar_desconto(valor):
    return valor * 0.9
```

---

### ✔️ 3. Poucos parâmetros (ideal: até 3)

Muitos parâmetros indicam responsabilidade excessiva.

❌ Ruim:
```python
def criar_usuario(nome, idade, cpf, email, telefone):
```

✅ Melhor:
```python
def criar_usuario(dados_usuario):
```

---

### ✔️ 4. Retorne valores, evite `print`

Função **calcula**, quem chama decide o que fazer.

❌ Ruim:
```python
def soma(a, b):
    print(a + b)
```

✅ Bom:
```python
def soma(a, b):
    return a + b
```

---

### ✔️ 5. Seja previsível

Mesma entrada → mesma saída (sempre que possível).

Evite efeitos colaterais escondidos.

---

### ✔️ 6. Use `docstring` quando necessário

```python
def calcular_media(valores):
    """
    Calcula a média de uma lista de números.

    :param valores: lista de números
    :return: média aritmética
    """
    return sum(valores) / len(valores)
```

---

## 🧱 Estrutura recomendada de uma função

```python
def nome_claro(parametro1, parametro2):
    # validações
    # lógica principal
    return resultado
```

---

## 🧠 Checklist final

Antes de escrever uma função, pergunte:

- [ ] Ela faz apenas uma coisa?
- [ ] O nome explica o que ela faz?
- [ ] Tem poucos parâmetros?
- [ ] Retorna um valor útil?
- [ ] Facilita manutenção e leitura?

Se sim → você escreveu uma **boa função** ✅

---

## 📌 Conclusão

Funções bem escritas tornam o código:

- Mais legível
- Mais reutilizável
- Mais fácil de testar
- Mais profissional

> *Código limpo não é sobre escrever menos código, é sobre escrever código com intenção.*

---

🚀 **Pratique refatorando código grande em pequenas funções bem nomeadas.**

