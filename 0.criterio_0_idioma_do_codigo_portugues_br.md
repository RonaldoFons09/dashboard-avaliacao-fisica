# Critério 0 — Idioma do Código (Português-BR)

## Definição

Todo o código — incluindo **variáveis, funções, classes, métodos e comentários** — deve ser escrito em **Português do Brasil (PT-BR)**.

O objetivo é garantir **clareza, alinhamento da equipe e compreensão imediata**, evitando barreiras linguísticas desnecessárias.

---

## Princípio central

Código é uma forma de **comunicação entre pessoas**, não apenas uma instrução para o computador.

Quando o time pensa, discute regras de negócio e documenta decisões em português, escrever o código no mesmo idioma:

- reduz ambiguidade
- diminui erros de interpretação
- acelera entendimento
- facilita onboarding de novos desenvolvedores

---

## O que este critério exige

Todos os identificadores definidos pelo desenvolvedor devem estar em **Português-BR**:

- variáveis
- funções e métodos
- classes
- módulos internos
- comentários e docstrings

📌 **Exceção**: nomes vindos de bibliotecas, frameworks ou APIs externas **não devem ser traduzidos**.

---

## Boas práticas associadas

### ✔️ Variáveis

❌ Ruim:
```python
user_age = 30
```

✅ Bom:
```python
idade_usuario = 30
```

---

### ✔️ Funções

❌ Ruim:
```python
def calculate_total(price, quantity):
    return price * quantity
```

✅ Bom:
```python
def calcular_total(preco, quantidade):
    return preco * quantidade
```

---

### ✔️ Classes

❌ Ruim:
```python
class OrderManager:
    ...
```

✅ Bom:
```python
class GerenciadorPedidos:
    ...
```

---

## Checklist de avaliação

Use as perguntas abaixo para validar este critério:

- [ ] Variáveis e funções estão em Português-BR?
- [ ] Os nomes refletem termos usados no domínio do negócio?
- [ ] Não há mistura desnecessária de idiomas?
- [ ] As exceções (bibliotecas externas) estão sendo respeitadas?

Se alguma resposta for **não**, o critério não está sendo atendido.

---

## Frase-guia

> **Se o time pensa em português, o código deve falar português.**

---

## Objetivo do critério

Padronizar o idioma do código para:

- aumentar legibilidade
- reduzir carga cognitiva
- facilitar comunicação
- alinhar código com o domínio do problema

Idioma consistente também é uma forma de qualidade de software.

